---
name: univer-worklog-append
description: Use when the user wants to manually append their own worklog or daily report row into the Univer workbook. Triggers include worklog-append, dogfooding-append, append, 记录进展, 手动写日报.
---

# univer-worklog-append

Use the core protocol in `../univer-team-standup/SKILL.md`, route intent `append`.

Follow the core rules for profile onboarding, workbook reads/writes, priority resolution, dedupe key generation, audit logging, preview, and no default commit/sync. Always follow the core Team Remote policy: append writes and any requested publish/sync must target only `fYmh0HRyTUO6YECQGFScnA0` on `https://univer.ai/`; re-check the bound unit id before pull, before writing, before commit/sync, and after sync. Always follow the core Personal Log Date Field Policy: write `日期` as the local work date, apply `yyyy-mm-dd` display formatting, and verify the date display is not a naked spreadsheet serial number. Always follow the core Progress And Risk Prompting Policy: before writing, remind the user to confirm or fill progress, risk, blocker, plan, and next-action fields.

During 2026-05-21 through 2026-06-18, apply the core Business Goal Tracking rules when the manual note clearly maps to an O1-O4 KR. Do not force a goal mapping for vague notes.
